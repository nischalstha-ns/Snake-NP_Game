
export const GRID_SIZE = 20; // Number of cells in width/height
export const INITIAL_GAME_SPEED_MS = 150; // Milliseconds
export const BASE_CELL_SIZE_PX = 20; // Default cell size if screen is very large
export const MIN_CELL_SIZE_PX = 10; // Minimum cell size for small screens
export const MIN_GAME_SPEED_MS = 50; // Fastest game speed in milliseconds
export const SPEED_INCREMENT_FACTOR = 0.97; // e.g., 0.97 for 3% speed increase per food
